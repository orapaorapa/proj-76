
// Définition de type pour les produits récupérés depuis SQL
export interface SqlProduct {
  id: string;
  description: string;
  clarte: string;
  couleur: string;
  taille: string;
  dimensions: string;
  prix: string;
}
